ponos v1.3.0 Release Notes
==========================

	* Introduce max_concurrent option for load generators limiting how
	  many concurrently ongoing tasks the load generator may spawn.

	* Add resources links in README.md
